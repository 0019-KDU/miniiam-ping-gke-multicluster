# 🔄 Traffic Flow Analysis - Ping IAM Multi-Cluster Architecture

## 📊 Architecture Overview: GKE Gateway API + Istio Envoy Sidecar Proxy 

Your architecture uses a **two-layer proxy model**:

| Layer | Component | Role |
|-------|-----------|------|
| **Layer 1 (Ingress)** | GKE Gateway API + Google Cloud L7 LB | External traffic routing (North-South) |
| **Layer 2 (Mesh)** | Istio Envoy Sidecar Proxies | Service-to-service mTLS (East-West) |

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                                    INTERNET                                              │
└─────────────────────────────────────┬───────────────────────────────────────────────────┘
                                      │
                                      │ NORTH-SOUTH TRAFFIC
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  ╔═══════════════════════════════════════════════════════════════════════════════════╗  │
│  ║                    LAYER 1: GKE GATEWAY API (Ingress)                             ║  │
│  ╠═══════════════════════════════════════════════════════════════════════════════════╣  │
│  ║  Google Cloud External L7 Load Balancer                                           ║  │
│  ║  • GatewayClass: gke-l7-global-external-managed-mc (Multi-Cluster)                ║  │
│  ║  • External IP: 34.36.200.69                                                    ║  │
│  ║  • Protocol: HTTP (Port 80)                                                       ║  │
│  ║  • Routing: HTTPRoute resources define path-based routing                         ║  │
│  ╚═══════════════════════════════════════════════════════════════════════════════════╝  │
└─────────────────────────────────────┬───────────────────────────────────────────────────┘
                                      │
          ┌───────────────────────────┼───────────────────────────────────┐
          │                           │                                   │
          ▼                           ▼                                   ▼
   ┌──────────────┐           ┌──────────────┐                   ┌──────────────┐
   │    /*        │           │  /.well-known│                   │   /pf, /as   │
   │  Main Route  │           │  OIDC Config │                   │  Auth Flows  │
   └──────┬───────┘           └──────┬───────┘                   └──────┬───────┘
          │                          │                                   │
          │                          └───────────────┬───────────────────┘
          │                                          │
┌─────────┴──────────────────────────────────────────┴────────────────────────────────────┐
│  ╔═══════════════════════════════════════════════════════════════════════════════════╗  │
│  ║                    LAYER 2: ISTIO ENVOY SIDECAR PROXIES (Mesh)                    ║  │
│  ╠═══════════════════════════════════════════════════════════════════════════════════╣  │
│  ║  Each Pod has an Envoy sidecar for:                                               ║  │
│  ║  • mTLS encryption (service-to-service)                                           ║  │
│  ║  • Traffic observability (metrics, tracing)                                       ║  │
│  ║  • Load balancing & circuit breaking                                              ║  │
│  ╚═══════════════════════════════════════════════════════════════════════════════════╝  │
└─────────────────────────────────────────────────────────────────────────────────────────┘
          │                                          │
          ▼                                          ▼
┌─────────────────┐                       ┌─────────────────────┐
│  PINGACCESS     │                       │   PINGFEDERATE      │
│  ┌───────────┐  │                       │   ┌───────────┐     │
│  │  Envoy    │  │   EAST-WEST TRAFFIC   │   │  Envoy    │     │
│  │  Sidecar  │◄─┼───────(mTLS)──────────┼──►│  Sidecar  │     │
│  └───────────┘  │                       │   └───────────┘     │
│  (Port 3000)    │                       │   (Port 9031)       │
│  API Gateway    │                       │   OIDC Provider     │
└────────┬────────┘                       └──────────┬──────────┘
         │                                           │
    ┌────┴────────────────┐                          │
    │                     │                          ▼
    ▼                     ▼               ┌─────────────────────┐
┌─────────────┐    ┌──────────────┐       │   PINGDIRECTORY     │
│  REACT-APP  │    │ BACKEND-API  │       │   ┌───────────┐     │
│ ┌─────────┐ │    │ ┌──────────┐ │       │   │  Envoy    │     │
│ │ Envoy   │ │    │ │  Envoy   │ │       │   │  Sidecar  │     │
│ │ Sidecar │ │    │ │  Sidecar │ │       │   └───────────┘     │
│ └─────────┘ │    │ └──────────┘ │       │   Ports 1389/1636   │
│  (Port 80)  │    │ (Port 8080)  │       │   BYPASSED by Envoy │
│  Frontend   │    │ API Server   │       └──────────┬──────────┘
└─────────────┘    └──────────────┘                  │
                                          ┌──────────┴──────────┐
                                          │   Cross-Cluster     │
                                          │   LDAP Replication  │
                                          │   (Raw TCP - No     │
                                          │    Envoy Proxy)     │
                                          └─────────────────────┘
```

---

## � Two-Layer Proxy Model Explained

### Why This Architecture?

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                           COMPARISON: SINGLE vs TWO-LAYER PROXY                          │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                          │
│  ❌ NOT YOUR ARCHITECTURE (Single Layer - Istio Ingress Gateway):                       │
│                                                                                          │
│     Internet → Istio IngressGateway (Envoy) → Services                                  │
│                      ↓                                                                   │
│              (One Envoy handles both ingress AND mesh routing)                          │
│                                                                                          │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                          │
│  ✅ YOUR ARCHITECTURE (Two Layer - GKE Gateway + Istio Sidecars):                       │
│                                                                                          │
│     Internet → GKE Gateway API (Google L7 LB) → Envoy Sidecars → Services              │
│                      ↓                                ↓                                  │
│              (Managed by Google)           (Managed by Istio)                           │
│              North-South Traffic           East-West Traffic                            │
│                                                                                          │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

### Layer 1: GKE Gateway API (North-South Traffic)

| Aspect | Details |
|--------|---------|
| **Component** | Google Cloud External HTTP(S) Load Balancer |
| **GatewayClass** | `gke-l7-global-external-managed-mc` |
| **Scope** | Multi-cluster (routes to any cluster in fleet) |
| **Protocol** | HTTP/HTTPS (Layer 7) |
| **Managed By** | Google Cloud (fully managed) |
| **Purpose** | External ingress, path-based routing, SSL termination |

```yaml
# Your Gateway Configuration
apiVersion: gateway.networking.k8s.io/v1beta1
kind: Gateway
metadata:
  name: ping-iam-gateway
spec:
  gatewayClassName: gke-l7-global-external-managed-mc  # ← Google-managed, NOT Envoy
  listeners:
    - name: http
      protocol: HTTP
      port: 80
```

### Layer 2: Istio Envoy Sidecar Proxies (East-West Traffic)

| Aspect | Details |
|--------|---------|
| **Component** | Envoy proxy injected as sidecar in each pod |
| **Managed By** | Cloud Service Mesh (Managed Istio) |
| **Protocol** | TCP/HTTP with mTLS encryption |
| **Purpose** | Service-to-service security, observability, traffic control |

```yaml
# Pod Annotation enabling Envoy Sidecar
metadata:
  annotations:
    sidecar.istio.io/inject: "true"  # ← Injects Envoy sidecar
```

---

## 🌐 External Traffic Flow (North-South)

### 1️⃣ Main Application Flow (/* → PingAccess)

```
User Browser
    │
    │  HTTP Request (Port 80)
    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  LAYER 1: GKE Gateway API                                               │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  Google Cloud External L7 Load Balancer                           │  │
│  │  • NOT Envoy - This is Google's own proxy infrastructure          │  │
│  │  • Global anycast IP: 34.36.200.69                              │  │
│  │  • HTTPRoute: pingaccess-main-route (path: /*)                    │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────┬───────────────────────────────────┘
                                      │
                                      │ Routes to Service (no Envoy yet)
                                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  LAYER 2: Envoy Sidecar Intercepts Traffic                              │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  PingAccess Pod                                                   │  │
│  │  ┌─────────────────┐      ┌─────────────────────────────────┐     │  │
│  │  │  Envoy Sidecar  │ ───► │  PingAccess Container           │     │  │
│  │  │  (istio-proxy)  │      │  (Port 3000)                    │     │  │
│  │  │                 │      │                                 │     │  │
│  │  │  • Terminates   │      │  • Policy Enforcement           │     │  │
│  │  │    mTLS from    │      │  • Session Management           │     │  │
│  │  │    other pods   │      │  • Route to backends            │     │  │
│  │  └─────────────────┘      └─────────────────────────────────┘     │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                  │
    ┌─────────────┼─────────────┐
    │             │             │
    ▼             ▼             ▼
┌────────┐  ┌──────────┐  ┌────────────────┐
│ /      │  │ /api/*   │  │ Unauthenticated│
│ React  │  │ Backend  │  │ → Redirect to  │
│ App    │  │ API      │  │   PingFederate │
└────────┘  └──────────┘  └────────────────┘
```

### 2️⃣ OIDC Authentication Flow

```
User (Unauthenticated)
    │
    │  1. Request Protected Resource
    ▼
┌─────────────────────────────────────────┐
│  PingAccess Engine                      │
│  • Detects: No valid session            │
│  • Action: Redirect to OIDC Provider    │
└─────────────────┬───────────────────────┘
                  │
                  │  2. OIDC Authorization Request
                  ▼
┌─────────────────────────────────────────┐
│  GKE Gateway HTTPRoute                  │
│  Paths: /.well-known/*, /pf/*, /as/*    │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  PingFederate Engine (Port 9031)        │
│  ┌───────────────────────────────────┐  │
│  │ Endpoints:                        │  │
│  │ • /.well-known/openid-config      │  │
│  │ • /as/authorization.oauth2       │  │
│  │ • /as/token.oauth2               │  │
│  │ • /pf/JWKS                        │  │
│  └───────────────────────────────────┘  │
└─────────────────┬───────────────────────┘
                  │
                  │  3. LDAP Authentication
                  ▼
┌─────────────────────────────────────────┐
│  PingDirectory (Ports 1389/1636)        │
│  ┌───────────────────────────────────┐  │
│  │ • Validate Username/Password      │  │
│  │ • Lookup User Attributes          │  │
│  │ • Return Group Memberships        │  │
│  │ Base DN: dc=example,dc=com        │  │
│  └───────────────────────────────────┘  │
└─────────────────┬───────────────────────┘
                  │
                  │  4. Return OAuth Tokens
                  ▼
┌─────────────────────────────────────────┐
│  PingFederate → User Browser            │
│  • ID Token (JWT)                       │
│  • Access Token (JWT)                   │
│  • Refresh Token                        │
└─────────────────┬───────────────────────┘
                  │
                  │  5. Redirect back to PingAccess
                  ▼
┌─────────────────────────────────────────┐
│  PingAccess Engine                      │
│  • Validates tokens with PingFederate   │
│  • Creates Session                      │
│  • Injects User Headers                 │
│  • Forwards to Backend                  │
└─────────────────────────────────────────┘
```

---

## 🔗 Internal Service Communication (East-West Traffic)

### Envoy Sidecar-to-Sidecar mTLS

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                    ISTIO SERVICE MESH (East-West Traffic)                     │
│                                                                               │
│   Traffic between services is intercepted by Envoy sidecars on BOTH ends     │
│                                                                               │
│   ┌─────────────────────────────────┐    ┌─────────────────────────────────┐ │
│   │       PingAccess Pod            │    │       React-App Pod             │ │
│   │   ┌─────────┐  ┌─────────────┐  │    │  ┌─────────────┐  ┌─────────┐  │ │
│   │   │ PA      │  │   Envoy     │  │    │  │   Envoy     │  │ React   │  │ │
│   │   │Container│◄─│   Sidecar   │◄─┼────┼──│   Sidecar   │◄─│ App     │  │ │
│   │   │         │  │             │  │    │  │             │  │         │  │ │
│   │   └─────────┘  └─────────────┘  │    │  └─────────────┘  └─────────┘  │ │
│   └─────────────────────────────────┘    └─────────────────────────────────┘ │
│                                                                               │
│                            mTLS Encrypted                                     │
│                    (automatic certificate rotation)                           │
│                                                                               │
│   ┌─────────────────────────────────┐    ┌─────────────────────────────────┐ │
│   │       Backend-API Pod           │    │     PingFederate Pod            │ │
│   │   ┌─────────┐  ┌─────────────┐  │    │  ┌─────────────┐  ┌─────────┐  │ │
│   │   │ API     │  │   Envoy     │  │    │  │   Envoy     │  │   PF    │  │ │
│   │   │Container│◄─│   Sidecar   │◄─┼────┼──│   Sidecar   │◄─│Container│  │ │
│   │   │:8080    │  │             │  │mTLS│  │             │  │ :9031   │  │ │
│   │   └─────────┘  └─────────────┘  │    │  └─────────────┘  └─────────┘  │ │
│   └─────────────────────────────────┘    └─────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────┘
```

### How Envoy Sidecar Intercepts Traffic

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      POD INTERNAL TRAFFIC FLOW                               │
│                                                                              │
│  1. INBOUND (Traffic entering the pod):                                      │
│     External → iptables REDIRECT → Envoy (15006) → App Container            │
│                                                                              │
│  2. OUTBOUND (Traffic leaving the pod):                                      │
│     App Container → iptables REDIRECT → Envoy (15001) → External            │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                         Backend-API Pod                                │ │
│  │                                                                        │ │
│  │    INBOUND                              OUTBOUND                       │ │
│  │    ────────                             ────────                       │ │
│  │    Network                              App Container                  │ │
│  │       │                                      │                         │ │
│  │       ▼                                      ▼                         │ │
│  │  ┌─────────────┐                       ┌─────────────┐                 │ │
│  │  │  iptables   │                       │  iptables   │                 │ │
│  │  │  REDIRECT   │                       │  REDIRECT   │                 │ │
│  │  └──────┬──────┘                       └──────┬──────┘                 │ │
│  │         ▼                                     ▼                        │ │
│  │  ┌─────────────────────────────────────────────────┐                   │ │
│  │  │              ENVOY SIDECAR (istio-proxy)        │                   │ │
│  │  │                                                 │                   │ │
│  │  │  Inbound Listener (15006)  Outbound Listener   │                   │ │
│  │  │         │                    (15001)            │                   │ │
│  │  │         │                         │             │                   │ │
│  │  │         ▼                         ▼             │                   │ │
│  │  │    [TLS Termination]      [TLS Origination]    │                   │ │
│  │  │    [Apply Policies]       [Apply Policies]     │                   │ │
│  │  │    [Collect Metrics]      [Collect Metrics]    │                   │ │
│  │  └─────────────────────────────────────────────────┘                   │ │
│  │         │                                                              │ │
│  │         ▼                                                              │ │
│  │  ┌──────────────┐                                                      │ │
│  │  │ App Container│                                                      │ │
│  │  │  (Port 8080) │                                                      │ │
│  │  └──────────────┘                                                      │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Backend API Token Validation Flow

```
Backend-API receives request
    │
    │  1. Check Headers (from PingAccess)
    ▼
┌─────────────────────────────────────────┐
│  Extract User from Headers              │
│  ┌───────────────────────────────────┐  │
│  │ • X-Forwarded-User                │  │
│  │ • X-Forwarded-Email               │  │
│  │ • X-Forwarded-Groups              │  │
│  │ • X-PA-Subject (alternative)      │  │
│  └───────────────────────────────────┘  │
└─────────────────┬───────────────────────┘
                  │
    ┌─────────────┴─────────────┐
    │                           │
    ▼                           ▼
┌───────────────┐       ┌───────────────────┐
│ Headers Found │       │ No Headers        │
│ Use PA-injected│      │ Check Auth Bearer │
│ user info     │       │ JWT Token         │
└───────────────┘       └─────────┬─────────┘
                                  │
                                  ▼
                        ┌───────────────────┐
                        │ Validate JWT      │
                        │ via JWKS from     │
                        │ PingFederate      │
                        └───────────────────┘
```

---

## 🌍 Multi-Cluster Traffic (GKE MCS)

### Cross-Cluster Service Discovery (Envoy NOT Involved in LDAP)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        GKE MULTI-CLUSTER SERVICES (MCS)                      │
│                                                                              │
│  ┌──────────────────────────────┐    ┌──────────────────────────────┐       │
│  │     GKE-ASIA (Primary)       │    │    GKE-EUROPE (Secondary)    │       │
│  │                              │    │                              │       │
│  │  ServiceExport (all):        │    │  ServiceExport (all):        │       │
│  │  • pingdirectory             │    │  • pingdirectory             │       │
│  │  • pingdirectory-cluster     │    │  • pingdirectory-cluster     │       │
│  │  • pingfederate-admin        │    │  • pingfederate-engine       │       │
│  │  • pingfederate-engine       │    │  • pingaccess-engine         │       │
│  │  • pingaccess-admin          │    │  • react-app                 │       │
│  │  • pingaccess-admin-cluster  │    │  • backend-api               │       │
│  │  • pingaccess-engine         │    │                              │       │
│  │  • react-app                 │    │  ExternalName (aliases):     │       │
│  │  • backend-api               │    │  • pingaccess-admin →        │       │
│  │                              │    │    *.clusterset.local        │       │
│  │                              │    │  • pingfederate-admin →      │       │
│  │                              │    │    *.clusterset.local        │       │
│  └──────────────────────────────┘    └──────────────────────────────┘       │
│                                                                              │
│  Cross-Cluster DNS Resolution:                                               │
│  • Local:      <svc>.ping-iam.svc.cluster.local                             │
│  • Remote:     <svc>.ping-iam.svc.clusterset.local                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

### PingDirectory LDAP Replication (Envoy Bypassed)

```
┌────────────────────────────────────────────────────────────────────────────────┐
│                    LDAP REPLICATION - ENVOY BYPASS                              │
│                                                                                 │
│  LDAP ports are EXCLUDED from Envoy sidecar proxy via:                         │
│  1. Pod annotation: traffic.sidecar.istio.io/excludeOutboundPorts              │
│  2. PeerAuthentication: portLevelMtls mode: DISABLE                            │
│  3. DestinationRule: tls mode: DISABLE                                         │
│                                                                                 │
└────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────┐        ┌─────────────────────────────────┐
│          GKE-ASIA               │        │         GKE-EUROPE              │
│                                 │        │                                 │
│  ┌───────────────────────────┐  │        │  ┌───────────────────────────┐  │
│  │    PingDirectory Pod      │  │        │  │    PingDirectory Pod      │  │
│  │                           │  │        │  │                           │  │
│  │  ┌─────────┐ ┌─────────┐  │  │        │  │  ┌─────────┐ ┌─────────┐  │  │
│  │  │ Envoy   │ │   PD    │  │  │  LDAP  │  │  │ Envoy   │ │   PD    │  │  │
│  │  │ Sidecar │ │Container│◄─┼──┼────────┼──┼─►│ Sidecar │ │Container│  │  │
│  │  │         │ │         │  │  │  Raw   │  │  │         │ │         │  │  │
│  │  │ (HTTP   │ │ Ports:  │  │  │  TCP   │  │  │ (HTTP   │ │ Ports:  │  │  │
│  │  │  only)  │ │ 1389 ───┼──┼──┼────────┼──┼──┼── 1389  │ │ 1636    │  │  │
│  │  │         │ │ 1636 ───┼──┼──┼BYPASSED┼──┼──┼── 1636  │ │ 8989    │  │  │
│  │  │         │ │ 8989 ───┼──┼──┼────────┼──┼──┼── 8989  │ │         │  │  │
│  │  └─────────┘ └─────────┘  │  │        │  │  └─────────┘ └─────────┘  │  │
│  └───────────────────────────┘  │        │  └───────────────────────────┘  │
│                                 │        │                                 │
└─────────────────────────────────┘        └─────────────────────────────────┘

WHY BYPASS ENVOY FOR LDAP?
─────────────────────────────
1. LDAP is a binary protocol - Envoy can't inspect/proxy it effectively
2. LDAP has its own TLS (LDAPS on 1636) - double encryption is wasteful
3. Replication requires direct TCP connections for reliability
4. PingDirectory handles its own authentication/encryption
```

---

## 🔐 Security & Access Control

### Two-Layer Security Model

| Layer | Security Mechanism | Managed By |
|-------|-------------------|------------|
| **Layer 1: GKE Gateway** | Cloud Armor, SSL Policies, IAP | Google Cloud |
| **Layer 2: Envoy Sidecars** | mTLS, AuthorizationPolicy | Istio/Cloud Service Mesh |

### Istio mTLS Configuration (Envoy Sidecar Behavior)

| Service | HTTP Traffic (via Envoy) | LDAP Ports (Envoy Bypassed) |
|---------|-------------------------|----------------------------|
| PingDirectory | PERMISSIVE (mTLS or plaintext) | DISABLED (raw TCP - port exclusion) |
| PingFederate | STRICT (mTLS required) | N/A |
| PingAccess | STRICT (mTLS required) | N/A |
| React-App | STRICT (mTLS required) | N/A |
| Backend-API | STRICT (mTLS required) | N/A |

### Internal Load Balancers (Admin Access)

```
┌─────────────────────────────────────────────────────────────────┐
│              INTERNAL VPC ACCESS ONLY                            │
│                                                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │ PingFederate    │  │ PingAccess      │  │ PingDirectory   │  │
│  │ Admin ILB       │  │ Admin ILB       │  │ Admin ILB       │  │
│  │ Port: 9999      │  │ Port: 9000      │  │ Ports: 1443,    │  │
│  │                 │  │                 │  │        1636     │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
│                                                                  │
│  Annotation: networking.gke.io/load-balancer-type: "Internal"   │
│  Global Access: Enabled (cross-region admin access)             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📝 Port Summary

| Service | Port | Protocol | Description |
|---------|------|----------|-------------|
| **Gateway** | 80 | HTTP | External ingress |
| **PingAccess Engine** | 3000 | HTTP | Runtime proxy |
| **PingAccess Admin** | 9000 | HTTPS | Admin console (internal) |
| **PingFederate Engine** | 9031 | HTTPS | OIDC runtime |
| **PingFederate Admin** | 9999 | HTTPS | Admin console (internal) |
| **PingDirectory** | 1389 | LDAP | Directory access |
| **PingDirectory** | 1636 | LDAPS | Secure directory |
| **PingDirectory** | 1443 | HTTPS | Admin console |
| **PingDirectory** | 8989 | TCP | Replication |
| **React-App** | 80 | HTTP | Frontend |
| **Backend-API** | 8080 | HTTP | REST API |

---

## 🔄 Complete Request Lifecycle

```
1. User opens https://app.example.com/
        │
        ▼
2. GKE Gateway routes to PingAccess (Port 3000)
        │
        ▼
3. PingAccess checks session
        │
   ┌────┴────┐
   │         │
   ▼         ▼
4a. No Session           4b. Valid Session
    │                         │
    ▼                         │
5a. Redirect to               │
    PingFederate              │
    /as/authorization         │
    │                         │
    ▼                         │
6a. PingFederate              │
    Login Page                │
    │                         │
    ▼                         │
7a. PingDirectory             │
    LDAP Auth                 │
    │                         │
    ▼                         │
8a. Return Tokens             │
    ID/Access/Refresh         │
    │                         │
    ▼                         │
9a. PingAccess                │
    Creates Session           │
    │                         │
    └─────────┬───────────────┘
              │
              ▼
10. PingAccess injects headers:
    • X-Forwarded-User
    • X-Forwarded-Email
    • X-Forwarded-Groups
              │
              ▼
11. Route to destination:
    • / → React-App (Port 80)
    • /api/* → Backend-API (Port 8080)
              │
              ▼
12. Backend-API validates:
    • Headers from PingAccess
    • OR JWT token via JWKS
              │
              ▼
13. Return Response to User
```

---

## 📊 Traffic Metrics & Observability

### What Each Layer Provides

| Layer | Observability Tool | Metrics Available |
|-------|-------------------|-------------------|
| **GKE Gateway** | Cloud Monitoring, Cloud Logging | Request count, latency, HTTP status codes, backend health |
| **Envoy Sidecars** | Kiali, Prometheus, Jaeger | Service graph, mTLS status, distributed tracing, request/response metrics |

### Traffic Visibility Matrix

| Traffic Type | GKE Gateway Metrics | Envoy/Kiali Metrics |
|--------------|---------------------|---------------------|
| Internet → Gateway | ✅ Yes | ❌ No |
| Gateway → Service | ✅ Yes | ✅ Yes (inbound to sidecar) |
| Service → Service | ❌ No | ✅ Yes (full mTLS telemetry) |
| LDAP Replication | ❌ No | ❌ No (bypasses Envoy) |

Note: LDAP replication traffic (ports 1389, 1636, 8989) is excluded from Envoy proxy and won't appear in service mesh metrics. Monitor via PingDirectory's native replication status.

---

## 📝 Summary: Your Two-Layer Proxy Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          TRAFFIC FLOW SUMMARY                                │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  INTERNET (Users)                                                    │   │
│   └─────────────────────────────────┬───────────────────────────────────┘   │
│                                     │                                        │
│                                     ▼                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  LAYER 1: GKE Gateway API                                           │   │
│   │  • Google Cloud L7 Load Balancer (NOT Envoy/Istio)                  │   │
│   │  • GatewayClass: gke-l7-global-external-managed-mc                  │   │
│   │  • Purpose: North-South ingress, path routing, multi-cluster        │   │
│   └─────────────────────────────────┬───────────────────────────────────┘   │
│                                     │                                        │
│                                     ▼                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  LAYER 2: Istio Envoy Sidecar Proxies                               │   │
│   │  • Injected into each pod (sidecar.istio.io/inject: "true")         │   │
│   │  • Managed by: Cloud Service Mesh (Managed Istio)                   │   │
│   │  • Purpose: East-West mTLS, observability, traffic policies         │   │
│   │  • EXCEPTION: LDAP ports excluded (raw TCP for replication)         │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│   KEY INSIGHT:                                                               │
│   ─────────────                                                              │
│   You are NOT using Istio IngressGateway (Envoy) for ingress.               │
│   Instead, GKE Gateway API provides ingress, and Istio only provides        │
│   sidecar proxies for service-to-service (mesh) traffic.                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

*Generated: Traffic Flow Analysis for Ping IAM Multi-Cluster Architecture*
*Architecture: GKE Gateway API + Istio Envoy Sidecar Integration*

---

## 📁 Current Project Structure

```
miniiam-ping-gke-multicluster/
├── docs/
│   ├── ARCHITECTURE.md              # System architecture and component design
│   ├── COMMANDS.md                  # All kubectl commands reference
│   ├── NETWORK.md                   # Network configuration guide
│   └── PING-STACK.md                # Ping Identity component integration
├── k8s/
│   ├── base/
│   │   ├── backend-api.yaml         # Backend API Deployment + Service
│   │   ├── devops-secret.yaml       # Ping DevOps credentials
│   │   ├── gateway.yaml             # GKE Gateway API + HealthCheckPolicy
│   │   ├── istio-config.yaml        # Istio PeerAuthentication & DestinationRules
│   │   ├── kustomization.yaml       # Kustomize base
│   │   ├── namespace.yaml           # ping-iam namespace
│   │   ├── react-app.yaml           # React frontend Deployment + Service
│   │   └── service-export.yaml      # MCS ServiceExports for cross-cluster discovery
│   └── overlays/
│       ├── gke-asia/                # PRIMARY CLUSTER (Seed)
│       │   ├── values-pingdirectory.yaml
│       │   └── values-ping-full.yaml
│       └── gke-europe/              # SECONDARY CLUSTER (Non-Seed)
│           ├── cross-cluster-services.yaml
│           ├── values-pingdirectory.yaml
│           └── values-ping-full.yaml
├── ping-iam-lab/                    # Docker Compose local development
├── README.md                        # Main documentation
└── TRAFFIC-FLOW.md                  # This file
```

### Cluster Role Summary

| Cluster | Role | Components |
|---------|------|------------|
| **gke-asia** | Primary/Seed | PingDirectory (seed), PingFederate Admin+Engine, PingAccess Admin+Engine |
| **gke-europe** | Secondary | PingDirectory (replica), PingFederate Engine, PingAccess Engine |

Note: Admin consoles are centralized in gke-asia. Engines in gke-europe connect to admins via MCS `clusterset.local` DNS.
